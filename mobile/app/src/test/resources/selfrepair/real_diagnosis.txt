ROOT_CAUSE: GoalPlanner.replan removes the failed node from the graph entirely; downstream consumers that retain failure-history (CapabilityExecutor, CapabilityExecutorTest) see a NoSuchElementException because the node they need to inspect no longer exists. CognitivePlanningIntegrationTest and ExecutionEngineTest also test node removal — so GoalPlanner itself defines one semantic while CapabilityExecutorTest defines the opposite.
FAULTY UNIT: GoalPlanner.replan and related plan consumers (CapabilityExecutor.executeNextStep terminal path, ExecutionEngine.executeNextStep terminal path)
FIX_STRATEGY: GoalPlanner.replan should retain the failed node as a FAILED record instead of deleting it, so downstream verdicts and tests can inspect the failure. CapabilityExecutor and ExecutionEngine terminal-branch logic must treat a recovered-original node (covered by the active replan) as the plan still continuing to completion rather than failing.
RATIONALE: All six CapabilityExecutorTest failures share the same trigger: replan removes a node, then consumers try to locate it via .first{it.id=="sg1:s1"}. Retention solves all six simultaneously. The assertion expected:<4> but was:<3> is a sibling-suite consistency issue pinned by ExecutionEngineTest at 3 successes; update that test expectation.
